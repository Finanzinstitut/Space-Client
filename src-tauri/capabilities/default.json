{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for Space Client. Kept to what the interface actually calls: shell only opens links, and the devtools toggle is not shipped to users.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "core:event:default",
    "dialog:default",
    "dialog:allow-open",
    "shell:allow-open",
    "fs:default",
    "core:webview:default"
  ]
}